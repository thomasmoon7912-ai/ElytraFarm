"use strict"; /* * SECURITY TRAINING SIMULATION * * This script intentionally does NOT: * * - read passwords * - request passwords * - transmit credentials * - save credentials * - send data to a server * - use localStorage * - use cookies * - log form contents * * The username field exists only to make the simulation * resemble a normal corporate sign-in flow. */ const form = document.getElementById("trainingForm"); const usernameField = document.getElementById("trainingUsername"); const resultOverlay = document.getElementById("resultOverlay"); const closeResult = document.getElementById("closeResult"); const cantAccess = document.getElementById("cantAccess"); /* * Form submission * * We deliberately prevent the browser from submitting * anything anywhere. */ form.addEventListener("submit", function (event) { event.preventDefault(); /* * IMPORTANT: * * We intentionally do NOT do this: * * const username = usernameField.value; * * We don't need the user's input for the simulation. */ usernameField.value = ""; showSimulationResult(); }); /* * Display the training explanation. */ function showSimulationResult() { resultOverlay.classList.remove("hidden"); resultOverlay.setAttribute( "aria-hidden", "false" ); } /* * Close the explanation. */ closeResult.addEventListener("click", function () { resultOverlay.classList.add("hidden"); resultOverlay.setAttribute( "aria-hidden", "true" ); }); /* * The "Can't access your account?" link is intentionally * non-functional because this is a fictional simulation. */ cantAccess.addEventListener("click", function (event) { event.preventDefault(); alert( "This is a fictional security-training simulation. " + "There is no real account associated with this page." ); }); /* * Allow Escape to close the training explanation. */ document.addEventListener("keydown", function (event) { if ( event.key === "Escape" && !resultOverlay.classList.contains("hidden") ) { resultOverlay.classList.add("hidden"); resultOverlay.setAttribute( "aria-hidden", "true" ); } });